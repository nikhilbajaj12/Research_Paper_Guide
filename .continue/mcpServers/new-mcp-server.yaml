name: DuckDuckGo MCP Server
version: 0.0.1
schema: v1

mcpServers:
  - name: DuckDuckGo MCP Server
    command: npx
    args:
      - -y
      - duckduckgo-mcp-server 
    env:
      DUCKDUCKGO_API_KEY: "<your_duckduckgo_api_key>" # If needed